## Guion Leccion 1: Introduccion al Desarrollo Web + Estructura HTML Basica

Hola y bienvenido. En esta leccion vas a aprender que es el desarrollo web, como funciona un sitio web y como escribir tu primera pagina en HTML. No necesitas instalar nada, solo abre tu navegador. Vamos.

Primero lo primero: que es exactamente un sitio web. Cuando abres una pagina como Wikipedia o YouTube en tu navegador, lo que estas viendo es el resultado de un archivo de texto especial que el navegador interpreta y muestra visualmente. Piensalo asi: un sitio web es como una carta. La carta tiene contenido, palabras, imagenes, listas. Pero tambien tiene formato, el tipo de letra, los colores, el tamanio del texto. En el desarrollo web, el contenido lo maneja HTML y el formato lo maneja CSS. En esta leccion nos enfocamos en HTML, que es la base de todo sitio web. Sin HTML no hay pagina web.

Ahora, que es HTML. HTML significa HyperText Markup Language. Lo que hace HTML es muy simple: le dice al navegador que es cada cosa en la pagina. Le dice: esto es un titulo, esto es un parrafo, esto es una imagen, esto es un enlace. HTML no es un lenguaje de programacion como Java. Es un lenguaje de estructura, defines que hay en la pagina y donde va. Lo hace con etiquetas.

Para escribir HTML no necesitas instalar nada. Vamos a usar CodePen, un editor online gratuito. Abre una nueva pestania y escribe: codepen.io/pen. Vas a ver tres columnas: HTML, CSS y JS. Hoy solo usamos la columna de HTML. En la parte de abajo veras el resultado en tiempo real cada vez que escribas algo.

Todo documento HTML tiene una estructura base que siempre es la misma. Primero DOCTYPE html que le dice al navegador que este archivo es HTML moderno. Luego la etiqueta html que contiene todo. Dentro va head con el titulo de la pagina, y body con todo el contenido visible. El h1 es el titulo principal y p es un parrafo de texto.

Una etiqueta tiene tres partes: etiqueta de apertura, contenido, y etiqueta de cierre. Juntas forman un elemento HTML. La etiqueta de cierre lleva una barra antes del nombre. Algunos elementos tambien tienen atributos, que son informacion extra dentro de la etiqueta de apertura. Por ejemplo un enlace usa href para decir a donde ir, y una imagen usa src para la direccion de la imagen y alt para el texto alternativo.

Pausa el video aqui. En CodePen escribe una pagina con un h1 con tu nombre, un p que diga algo sobre ti, y un enlace a tu red social favorita. Regresa cuando termines.

Antes de terminar te muestro los errores mas frecuentes. Error 1: olvidar cerrar la etiqueta, todo parrafo abierto con p debe cerrarse con barra p. Error 2: cerrar en el orden incorrecto, el ultimo en abrir es el primero en cerrar. Error 3: olvidar las comillas en los atributos, href siempre lleva comillas dobles. Si algo no aparece como esperas, revisa estas tres cosas.

Llegaste al final de la Leccion 1. Aprendiste que un sitio web tiene contenido en HTML y estilo en CSS. HTML usa etiquetas para definir cada elemento. La estructura base siempre incluye DOCTYPE html, html, head y body. Los atributos dan informacion extra a las etiquetas. En la proxima leccion veremos mas elementos HTML: encabezados, listas, imagenes y enlaces en detalle. El enlace a CodePen con el codigo de hoy esta en la descripcion. Nos vemos en la Leccion 2.
